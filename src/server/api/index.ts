// Placeholder API controllers directory for NOVA Studio
// These files are managed client-side and written to the user's local mirror when requested.

export const handlers = {
  hello: async () => ({ status: 200, body: { message: 'Hello from NOVA API (client-simulated)' } })
};
